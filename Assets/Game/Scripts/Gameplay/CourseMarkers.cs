namespace SpiderSwing.Gameplay
{
    // Marker file retained for project organization. Each MonoBehaviour now
    // lives in a same-named script file so Unity can serialize scene references
    // reliably.
}
